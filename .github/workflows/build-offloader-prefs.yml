name: Build Offloader Fixes

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git "$HOME/theos"
          brew install ldid make
          mkdir -p "$HOME/theos/sdks"
          git clone --depth 1 --filter=blob:none --sparse https://github.com/theos/sdks.git "$RUNNER_TEMP/theos-sdks"
          git -C "$RUNNER_TEMP/theos-sdks" sparse-checkout set iPhoneOS16.5.sdk
          cp -R "$RUNNER_TEMP/theos-sdks/iPhoneOS16.5.sdk" "$HOME/theos/sdks/"

      - name: Build preference bundle
        working-directory: source/OffloaderPrefsFix
        env:
          THEOS: ${{ github.workspace }}/../theos
        run: |
          export THEOS="$HOME/theos"
          gmake clean package FINALPACKAGE=1

      - name: Build vanilla context-menu capture
        working-directory: source/OffloaderVanillaCapture
        env:
          THEOS: ${{ github.workspace }}/../theos
        run: |
          export THEOS="$HOME/theos"
          gmake clean package FINALPACKAGE=1

      - name: Build runtime tweak
        working-directory: source/OffloaderRuntimeFix
        env:
          THEOS: ${{ github.workspace }}/../theos
        run: |
          export THEOS="$HOME/theos"
          gmake clean package FINALPACKAGE=1

      - name: Build AntiOffload tweak
        working-directory: source/OffloaderAntiOffloadFix
        env:
          THEOS: ${{ github.workspace }}/../theos
        run: |
          export THEOS="$HOME/theos"
          gmake clean package FINALPACKAGE=1

      - uses: actions/upload-artifact@v4
        with:
          name: offloader-fixes-build
          path: |
            source/OffloaderPrefsFix/packages/*.deb
            source/OffloaderVanillaCapture/packages/*.deb
            source/OffloaderRuntimeFix/packages/*.deb
            source/OffloaderAntiOffloadFix/packages/*.deb
